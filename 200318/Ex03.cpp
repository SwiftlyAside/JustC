#include <cstdio>
#include <conio.h>
#include <cstdlib>
#define ESC		0x1b

int main(int argc, char* argv[]) {
	/*char ch;
	while (true) {
	ch = _getch();
		if (ch == ESC) break;
		if (ch >= 'a' && ch <= 'z')
			ch = ch - 32;
		_putch(ch);
		printf("\n");
	}*/

	/*int i, sum = 0;
	while (_getch() != ESC) {
		scanf_s("%d", &i);
		if (/*i >= 1 && i <= 10 // 1 ~ 10 #1# 
			i >= 10 && i <= 20) {
			sum += i;
			printf("SUM : %d\n", sum);
		}
		else printf("Try again. \n");
	}*/
}
